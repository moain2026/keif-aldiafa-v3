# تدقيق SEO نقدي عميق — كيف الضيافة (keif-v2)
### Critical SEO Audit & Fix Plan

**التاريخ:** 2026-07-06 · **المنهجية:** فحص كود فعلي + بناء حي + curl + بحث في مصادر Google الرسمية.
**نطاق هذه الوثيقة:** تحليل ونقد وخطة إصلاح فقط (لم يُعدّل أي كود إنتاجي).

---

## الأدلة الحية المؤكّدة (بالفحص)

| الفحص | النتيجة |
|-------|---------|
| صور فعلية في HTML لـ /portfolio | 107 (تطابق مطالبة الـ sitemap) |
| صور فعلية في HTML لـ /offerings | 122 |
| صور فعلية في HTML لـ /services | 97 |
| صور في الصفحة الرئيسية (HTML أولي) | 2 (هيرو فيديو) |
| image sitemap | 388 `<image:loc>` عبر 4 صفحات ✅ |
| alt فارغ | صفر ✅ |
| مصدر توليد alt في المعرض | `PortfolioClient.tsx` سطر 343: قالب بالفئة + رقم |

---

## قائمة الثغرات مرتّبة بالأولوية

### 🔴 P0 — حرِج

**P0-1: alt مرئي مكرّر/قالبي (أقوى إشارة صور مهدورة).**
- الدليل: `PortfolioClient.tsx:343` → ``alt={`${seoAltMapping[item.category]} - لقطة ${idx + 1} من معرض أعمالنا`}``. كل صور نفس الفئة تحصل على alt متطابق عدا الرقم (80+ صورة فعالية بنفس النص).
- الأثر: جوجل يقرأ alt كأقوى إشارة لموضوع الصورة؛ التكرار = إشارة ضعيفة/مكرّرة، وتقليل فرص الظهور لكلمات متنوّعة في بحث الصور.
- الجذر: الوصف الفريد موجود في `imageCatalog.filenameToText()` لكنه يُستخدم في sitemap/JSON-LD فقط، وليس في `<img alt>` المرئي.
- الإصلاح: تمرير alt فريد لكل صورة من الكتالوج (مشتق من اسم الملف الوصفي) إلى المكوّنات (`ProtectedImage`/`OptimizedImage`) في `PortfolioClient`/`ServicesClient`/`OfferingsClient`. الحفاظ على طبيعية النص (لا حشو).

### 🟠 P1 — مهم

**P1-1: FAQPage schema لم يعد يُنتج نتائج غنية.**
- الدليل (مصدر رسمي): [Google Search updates](https://developers.google.com/search/updates#removing-faq-rich-result) — «FAQ rich result feature is no longer shown in Google Search results» (توقّف مايو 2026، حُذفت الوثائق يونيو 2026).
- الأثر: `generateFAQSchema` على صفحات المدن **لا يعطي نتيجة غنية** بعد الآن. غير ضار (بيانات صحيحة تساعد الفهم) لكنه ليس مكسباً كما كان يُظنّ. **تصحيح لتوقّع سابق خاطئ.**
- الإصلاح: إبقاء الأسئلة كمحتوى مرئي مفيد؛ إزالة الاعتماد عليها كـ«رich result»، والتركيز على تعميق المحتوى بدلاً منها.

**P1-2: عمق محتوى صفحات المدن (~725 كلمة) وخطر doorway pages.**
- الدليل: 8 صفحات بقالب متطابق بنيوياً، تختلف بالنص فقط. [Google spam policies — doorway pages](https://developers.google.com/search/docs/essentials/spam-policies#doorways) تحذّر من صفحات متعددة متشابهة موجّهة لمناطق بهدف الترتيب فقط.
- الأثر: المحتوى فريد (يقلّل الخطر) لكنه رفيع؛ عرضة لتقييم «قيمة مضافة منخفضة».
- الإصلاح: إضافة قيمة حقيقية لكل مدينة: صور مرتبطة، شهادات/أمثلة أعمال، معلومات محلية، أسئلة أعمق، روابط داخلية سياقية.

**P1-3: LocalBusiness بعنوان دولة فقط.**
- الدليل: `schema.ts` → `PostalAddress` فيه `addressCountry: "SA"` فقط (قرار مقصود لتغطية وطنية). [Google LocalBusiness](https://developers.google.com/search/docs/appearance/structured-data/local-business) يفضّل عنواناً كاملاً/إحداثيات للأهلية القوية.
- الأثر: مقايضة مقصودة (تغطية وطنية مقابل قوة النشاط المحلي). مقبول لكن يُوثّق.

### 🟡 P2 — تحسين

- **P2-1:** `ImageObject` في JSON-LD بلا `width`/`height` — جوجل يفضّلها للأهلية.
- **P2-2:** التحقق من `srcset` على صور المعرض. الشبكة تمرّر `sizes` + `fill` (next/image يولّد srcset)، لكن صورة الـ lightbox (`ProtectedImage fill` بلا `sizes`) قد لا تولّده. يُتحقّق ويُضاف `sizes`.
- **P2-3:** الصفحة الرئيسية بصورتين في HTML الأولي (بقية الصور JS/lazy) — مقبول للأداء ومغطّى بصفحات أخرى في الفهرسة.

---

## خطة الإصلاح المحكمة (للتنفيذ في مرحلة منفصلة)

1. **P0-1 (أولاً):** إضافة دالة alt فريدة في `imageCatalog.ts` (`altForSrc(src)`), وتمريرها في `PortfolioClient`/`ServicesClient`/`OfferingsClient` بدل القوالب. التحقق: curl → alt متنوّعة بلا تكرار.
2. **P2-1:** إضافة `width`/`height` (أو تقدير قياسي) لعناصر `ImageObject` في `generateImageGallerySchema`.
3. **P2-2:** تمرير `sizes` لصورة lightbox في `ProtectedImage`؛ التحقق من srcset في HTML.
4. **P1-1:** توثيق أن FAQ لم يعد rich result (إبقاء المحتوى، لا الاعتماد عليه).
5. **P1-2 (اختياري/أكبر):** تعميق محتوى صفحات المدن.

**ضمانات الأمان:** عدم لمس الألوان/النصوص التسويقية/`next.config` صور/`locations` قوالب التصميم؛ إبقاء `tsc --noEmit` = 0 و`npm run build` أخضر؛ التحقق الحي بعد كل خطوة؛ commit منفصل.

---

## الحكم العام

**التقييم: 8.5/10 تقنياً.** الأساس ممتاز ومطابق للمعايير الحالية (image sitemap صحيح، ربط صفحات↔صور سليم، بنية نظيفة). الحلقة الأضعف الحقيقية = **alt المرئي المكرّر (P0-1)** وهي أولوية التنفيذ الأولى. تصحيح مهم: **FAQ لم يعد rich result** (خطأ في توقّع سابق، مصدره الآن موثّق).
