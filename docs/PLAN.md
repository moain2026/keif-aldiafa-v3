# PLAN.md — إعادة بناء «كيف الضيافة» (feat/homepage-rebuild)

> عقد العمل الرقمي للفريق. يُحدَّث حصرياً بقرارات موثقة في docs/decisions/.
> المرجع: تكليف المالك (المواد 1-13) — #alabasi comet_message_id=2304516 بتاريخ 2026-07-27.

## 0) الحوكمة
- المنسّق وحَكَم القرارات: مدير المنتج. حارس البوابة: مراجع الكود.
- كل خلاف: نقاش موثق واحد في docs/decisions/DR-<n>.md → قرار مدير المنتج نهائي في نفس الجلسة.
- كل دورة عمل توثَّق في docs/loops/loop-<n>.md وكل دليل في docs/evidence/.
- ممنوع الدفع إلى main. كل الشغل على feat/homepage-rebuild.

## 1) ترجمة صوت المالك → مشاكل مقيسة (فرضيات تُثبت بالقياس في اللفة 1)
| صوت المالك | الترجمة القابلة للقياس (فرضية أولية — تُثبَّت أو تُعدَّل بأرقام اللفة 1) |
|---|---|
| «يدخل فيتشتّت، ما يلاقي المحتوى البصري» | أول شاشة (360px) لا تحتوي مسارات خدمات واضحة ولا محتوى بصري من أعمال حقيقية |
| «ما يلاقي كل شي» | لا يوجد قسم خدمات في متن الرئيسية (nav/footer فقط) — يُقاس بجرد أقسام الصفحة |
| «مرة مرتب / تنظيم ترتيب» | إيقاع تباعد غير متسق + طول صفحة زائد — يُقاس بلقطات 360/768/1024/1440 |
| «فخم فخامة دقة سلاسة» | معايير بصرية تُشتق من التحليل التنافسي (≥5 مواقع) وتثبَّت هنا كأرقام |
| «العميل يدخل على طول يشتي يتصل» | CTA اتصال/واتساب فوق الطية على كل العروض + مسار تواصل ≤ نقرتين |

## 2) معايير القبول (المادة 8 — تُقاس على بناء إنتاجي فقط)
- Lighthouse موبايل: Performance ≥85 · LCP ≤2.5s · TBT ≤300ms · CLS ≤0.1 · A11y = 100
- تباين ≥4.5:1 لكل نص · أهداف لمس ≥44px
- next build نظيف بلا تحذيرات · صفر أخطاء TypeScript
- صفر مسارات مكسورة (زحف موثق لكل صفحة/API/رابط)
- Schema تجتاز أدوات الفحص القياسية
- أهداف المحتوى/الكثافة البصرية: تُثبَّت في §4 بعد التحليل التنافسي (اللفة 1)

## 3) الأدوار (المادة 5)
- مدير المنتج: تحكيم، أولويات، هذا الملف، بوابة الدورات.
- مهندس + باني مواقع (الموقع الإلكتروني): التنفيذ على الفرع.
- مراجع الكود: بوابة الدمج — لا يمر PR/commit مرحلي بلا مراجعته.
- بحث معمق: التحليل التنافسي الداخلي/الخليجي + UX.
- باحث أكاديمي: التحليل التنافسي الخارجي العالمي بمصادر (بدأ فعلاً).
- عالم أبحاث: القياسات (Lighthouse/زحف/تباين) + QA بالأدلة.
- SEO: بنية العناوين/Schema/الكلمات — حماية الترتيب أثناء إعادة البناء.
- المصمم: النظام البصري + توقيع «صفر عيوب بصرية» مع مراجع الكود.
- صور AI / فيديو AI: أصول بصرية من CDN المشروع وخزنة الوسائط فقط.
- كاتب التقارير: docs/loops/ و docs/FINAL_REPORT.md.

## 4) العقد الرقمي للمحتوى والكثافة البصرية
(يُملأ في نهاية اللفة 1 من التحليل التنافسي — بند إلزامي قبل أي تنفيذ واجهة)

## 5) سجل الدورات
- Loop 1 (جارية): جرد + قياس أساس (baseline) + تحليل تنافسي → docs/loops/loop-1.md
