# تقرير خط الأساس — ضمان الجودة (QA-BASELINE)

- **التاريخ**: 2026-07-27 (UTC)
- **الفرع**: feat/homepage-rebuild (من main @ ce3aaf4)
- **المُعِدّ**: ضمان الجودة
- **بيئة القياس**: بناء إنتاجي (`next build` + `next start`) على منفذ محلي — لا قياسات dev إطلاقاً (المادة 9).
- **الأدوات**: Lighthouse 12 (Chromium headless)، زاحف مسارات مخصّص (Node fetch)، Playwright 1.62 للقطات والتصفح الحقيقي.

---

## 1) البناء الإنتاجي و TypeScript

| الفحص | النتيجة | الدليل |
|---|---|---|
| `npx tsc --noEmit` | ✅ صفر أخطاء (exit 0) | `docs/evidence/qa-baseline/tsc-output.txt` |
| `next build` | ✅ نجح (exit 0) — **لكن 4 تحذيرات ESLint** | `docs/evidence/qa-baseline/next-build-output.txt` |

### التحذيرات الأربعة (تُخالف معيار «build نظيف بلا تحذيرات» — المادة 8):
1. `src/app/layout.tsx:158` — سكربت GA مضمّن بدل `next/script` (`@next/next/next-script-for-ga`).
2. `src/components/DallahLogo.tsx:14` — `<img>` بدل `next/image` (`no-img-element`).
3. `src/components/ImageWithFallback.tsx:88` — صورة بلا `alt` (`jsx-a11y/alt-text`).
4. `src/components/ImageWithFallback.tsx:92` — صورة بلا `alt` (`jsx-a11y/alt-text`).

**الحكم: معيار «build نظيف» غير مستوفى حالياً.**

---

## 2) Lighthouse على البناء الإنتاجي — الصفحة الرئيسية

| المقياس | موبايل (قياس فعلي) | الهدف (المادة 8) | الحالة |
|---|---|---|---|
| Performance | **81** | ≥85 | ❌ فجوة 4 نقاط |
| LCP | **4.0s** | ≤2.5s | ❌ فجوة 1.5s |
| TBT | **290ms** | ≤300ms | ⚠️ حدّي (هامش 10ms فقط) |
| CLS | **0** | ≤0.1 | ✅ |
| Accessibility | **100** | 100 | ✅ |
| FCP | 0.9s | — | ✅ جيد |
| Speed Index | 2.0s | — | ✅ جيد |

سطح المكتب: Performance **97**، LCP 0.8s، TBT 40ms، A11y 100 — ✅ سليم.

الأدلة: `docs/evidence/qa-baseline/lighthouse-home-mobile.json` و `lighthouse-home-desktop.json`.

### الجذور المرصودة للفجوات (موبايل):
- **LCP 4.0s**: FCP ممتاز (0.9s) لكن عنصر LCP يتأخر ~3s — المشتبه الرئيسي: صورة Hero تُحمَّل عبر الشبكة بعد الـ JS، + مسار التحميل غير مُعطى أولوية كافية على اتصال موبايل مقيّد. (كوميت سابق 9054c50 حسّن LCP محلياً لكن القياس الحالي على throttling موبايل ما زال 4.0s.)
- **TBT 290ms**: عمل main-thread إجمالي 6.0s، bootup 1.5s. أكبر مساهم قابل للإزالة: **سكربتات Google Tag Manager/Ads غير مستخدمة بـ 124KiB** (`unused-javascript`: gtag G-ZZHYDVVMT1 = 70.8KiB + AW-11081441847 = 56.5KiB). نقل GA إلى `next/script` باستراتيجية مؤجلة يعالج التحذير + يخفض TBT معاً.

---

## 3) زحف المسارات — صفر أعطال داخلية ✅

- **63 مساراً داخلياً** (كل الصفحات الـ47 المولّدة + robots/sitemaps + مسارات مكتشفة من الروابط): **63/63 ⇒ HTTP 200**. صفر مكسور، صفر redirect.
- **88 رابطاً خارجياً** فريداً (CDN، شبكات اجتماعية، wa.me، خرائط…): **88/88 سليمة**.
- الأدلة: `docs/evidence/qa-baseline/crawl-report.json` و `external-links-report.json`.

---

## 4) الفحص البصري (المادة 7) — لقطات على 360/768/1024/1440 RTL

- اللقطات كاملة الطول: `docs/evidence/screens/qa-baseline/home-{360,768,1024,1440}.png` + `contact-360.png`.
- `dir="rtl"` مثبَّت على كل العروض ✅.
- **صفر horizontal overflow** على كل العروض (scrollWidth == clientWidth) ✅.
- **صفر أخطاء console** أثناء تحميل الرئيسية على كل العروض ✅.
- التفاصيل: `docs/evidence/screens/qa-baseline/visual-walkthrough.json`.

---

## 5) مسار الزائر الحقيقي (موبايل 360px أولاً)

- **فوق الطية مباشرة**: 3 أزرار تواصل ظاهرة — «واتساب» (84×44)، «اتصال» (75×44)، «احجز الآن» (104×54) ✅ الوصول للتواصل بلا احتكاك من أول شاشة.
- روابط التواصل بالرئيسية: 3×tel، 5×wa.me، 1×mailto — كلها صحيحة الرقم (+966508252134).
- **نموذج /contact**: يعمل client-side — يجمع (name*, phone*, email, date, service, message*) ويفتح واتساب برسالة مُنسّقة. المسار مكتمل وظيفياً ✅ (لا API خلفي — قرار معماري قائم، لا عطل).

### ⚠️ عيب مرصود — أهداف لمس < 44px (يخالف المادة 8: أهداف لمس ≥44px):
على الرئيسية بعرض 360px رُصد **40 عنصراً تفاعلياً** أبعاده أقل من 44px، أبرزها:
- أيقونات الهيدر الاجتماعية: 40×44 (العرض ناقص 4px).
- روابط قائمة الموبايل: 312×**36**.
- روابط الفوتر (المدن + صبابين قهوة): ارتفاع **28–36px**.
- رابط «تخطي إلى المحتوى» 1×1 (مقبول — عنصر وصولية مخفي بالتصميم، مستثنى).

ملاحظة: Lighthouse A11y=100 لأن فحصه يقيس التداخل لا الحجم المطلق؛ معيارنا الداخلي (≥44px) أصرم وهو المُلزم.

---

## 6) ملخص الفجوات المفتوحة (المدخل لدورة loop-1)

| # | الفجوة | القياس الحالي | الهدف | الجذر | المالك المقترح |
|---|---|---|---|---|---|
| G1 | Lighthouse Perf موبايل | 81 | ≥85 | LCP + TBT أدناه | مهندس |
| G2 | LCP موبايل | 4.0s | ≤2.5s | أولوية تحميل صورة Hero على الشبكات المقيّدة | مهندس |
| G3 | TBT موبايل | 290ms (حدّي) | ≤300ms بهامش آمن | 124KiB سكربتات gtag غير مستخدمة | مهندس |
| G4 | تحذيرات build ×4 | 4 | 0 | GA inline + img + alt مفقود | مهندس |
| G5 | أهداف لمس <44px | 40 عنصراً | 0 | قائمة الموبايل + الفوتر + أيقونات الهيدر | مصمم + مهندس |

**ما هو سليم ولا يُمَسّ إلا بحذر (خط انحدار)**: 63 مساراً تعمل، CLS=0، A11y=100، RTL سليم، صفر overflow، صفر أخطاء console، مسار التواصل مكتمل، سطح المكتب 97.

سأعيد نفس الحزمة القياسية كاملة بعد كل دورة (المادة 6) وأي تراجع في «خط الانحدار» أعلاه = عيب معطِّل.
