# 📚 سجل الأخطاء والإصلاحات الكامل — كيف الضيافة (keifaldiafa.com)

> توثيق شامل لكل خطأ اكتُشف وكيف أُصلح — مرجع خبرة دائم.
> يغطي جلسات: 2026-07-06 → 2026-07-13. مرتّب زمنياً بالـcommits.

---

## 🎯 الفلسفة العامة (الدرس الأكبر)

**لا نصدّق أي تقرير أو أداة أو مساعد أعمى — نتحقق بأدلة حية.**
كل إصلاح في هذا السجل مرّ بـ: (1) اكتشاف السبب الجذري، (2) إصلاح، (3) build أخضر (tsc+build)، (4) نشر Vercel، (5) **تحقق حي بـcurl/Tag Assistant**، (6) توثيق. لا ادعاء «تمام» بلا دليل.

---

## القسم الأول: أخطاء السيو والأداء (2026-07-06 → 07-08)

### ❌ خطأ 1: صور صفحات الخدمة×المدينة بلا حماية
- **الـcommit:** `b6066ce`
- **الخطأ:** الـ24 صفحة خدمة×مدينة كانت تستخدم `<Image>` عادية بلا `ProtectedImage`/علامة مائية.
- **السبب:** نُسخت من قالب قبل إضافة نظام الحماية.
- **الإصلاح:** استبدال كل `<Image>` بـ`ProtectedImage showWatermark`.
- **الدرس:** عند بناء صفحات متعددة من قالب، تأكد أن كل الميزات المشتركة (حماية/تتبّع) مطبّقة على الكل.

### ❌ خطأ 2: العلامة المائية مركزية ضخمة تفسد جمال الصور — ثم عكسها
- **الـcommits:** `c9b4f4a` (صغّرها ركنية) → `b4f416b` (أرجعها مركزية متوازنة)
- **الخطأ/التذبذب:** بدأت مركزية ضخمة (تخنق الصورة) → صغّرتها ركنية (ضعيفة الحماية) → عمر طلب مركزية للحماية.
- **الحل النهائي المتوازن:** مركزية 35% عرض + شفافية 30% + `mix-blend-mode: screen` + drop-shadow. حماية قوية بلا خنق الصورة.
- **الدرس:** قرارات UX ذات trade-off (جمال vs حماية) — اعرض الخيارات بأدلة ودع المالك يقرر، لا تتذبذب.

### ❌ خطأ 3: X-Robots-Tag: noindex يخفي الموقع (تاريخي — أُصلح قبل التوثيق)
- **الخطأ:** كان `X-Robots-Tag: noindex` مطبّقاً على كل نشر عدا `VERCEL_ENV==='production'`.
- **السبب:** إعداد افتراضي خاطئ في next.config.js.
- **الإصلاح:** `X-Robots-Tag: all` افتراضياً.
- **الأثر لو بقي:** الموقع **مخفي كلياً من جوجل**. أخطر خطأ ممكن.
- **الدرس:** افحص headers الفهرسة أول شي في أي تدقيق SEO.

### ❌ خطأ 4: رقم واتساب يمني (+967) في كريديت المطوّر
- **الـcommit:** `a864187`
- **الخطأ:** `Navbar.tsx:272` فيه `wa.me/967770941666` (رقم يمني) في كريديت «Alabbasi Soft».
- **السبب:** كود منسوخ من مشروع سابق.
- **الإصلاح:** إزالة الرابط، إبقاء «Alabbasi Soft» كنص.
- **الدرس:** أي رقم/رابط في موقع تجاري لازم يطابق هوية النشاط (سعودي)، خصوصاً NAP consistency.

### ❌ خطأ 5: lastmod مزيف في sitemap (يتغيّر كل زحف)
- **الـcommits:** `a864187` (ثابت موحّد) → `ae904fe` (git — فشل) → `26c9750` (خريطة ثابتة متنوّعة)
- **الخطأ:** `sitemap.ts` استخدم `new Date()` → كل صفحة «تعدّلت الآن» في كل زحف.
- **السبب:** توليد التاريخ وقت الطلب بدل تاريخ ثابت.
- **الإصلاح النهائي:** خريطة تواريخ ثابتة متنوّعة لكل قسم (تعكس آخر تعديل حقيقي).
- **محاولة فاشلة مفيدة:** جرّبت `git log %cs` runtime — فشل على Vercel لأنه **shallow clone (عمق 1)** فأعطى كل الصفحات نفس التاريخ.
- **الدرس:** (1) جوجل تتجاهل lastmod المزيّف. (2) Vercel يستنسخ git بعمق 1 — لا تعتمد على git log per-file وقت البناء.

### ❌ خطأ 6: preload صور الهيرو في كل الصفحات
- **الـcommit:** `a864187`
- **الخطأ:** `layout.tsx` يضع preload لصور الهيرو في root → كل صفحة داخلية تحمّل ~230KB بلا استخدام.
- **الإصلاح:** نقل preload إلى `page.tsx` (الرئيسية فقط).
- **الدرس:** preload في root layout يصيب كل الصفحات — ضعه في الصفحة المحددة فقط.

### ❌ خطأ 7: FAQPage schema مُهمل + 24 صفحة يتيمة
- **الـcommit:** `a864187`
- **الخطأ:** (أ) FAQPage schema (ألغته جوجل مايو 2026). (ب) الـ24 صفحة خدمة×مدينة غير مربوطة من أي مكان.
- **الإصلاح:** حذف FAQPage (إبقاء المحتوى المرئي) + قسم «خدماتنا في مدن المملكة» بالفوتر (24 رابط).
- **الدرس:** الصفحات المعزولة (orphan) لا يزحفها جوجل بجدية — اربطها من الفوتر/التنقل.

### ❌ خطأ 8: LocalBusiness ناقص + النوع الخاطئ
- **الـcommit:** `07dd734`
- **الخطأ:** `@type: ["ProfessionalService","FoodService"]` — **FoodService ليس LocalBusiness أصلاً!** + `address` بدولة فقط (بلا مدينة/geo).
- **السبب:** فهم خاطئ لأنواع Schema.org.
- **الإصلاح:** `@type: "CateringService"` (النوع الرسمي الصحيح لـSAB) + address بمستوى جدة + geo (21.4858, 39.1925) بدون streetAddress (صيغة SAB الصحيحة).
- **الدرس:** لنشاط متنقّل (Service Area Business): CateringService + areaServed + geo، بلا عنوان مكشوف.

### ❌ خطأ 9: priceRange بأرقام ريالية بلا موافقة العميل
- **الـcommit:** `4269780`
- **الخطأ:** Fable 5 وضع `priceRange: "SAR 500-5000"` وأنا نفّذته بلا التحقق من العميل.
- **السبب:** ثقة عمياء في مخرجات الوكيل الفرعي.
- **الإصلاح:** تراجع إلى `$$$` (النشاط مخصّص لكل مناسبة، لا باقات ثابتة).
- **الدرس:** لا تنشر أرقاماً/أسعاراً بلا تأكيد المالك — حتى لو «خبير» اقترحها.

### ❌ خطأ 10: sitemap عربي خام غير مُرمّز
- **الـcommit:** `07dd734`
- **الخطأ:** `<loc>` فيه `جدة` خام بينما canonical مُرمّز (`%D8%AC...`) — عدم اتساق يخالف مواصفة sitemaps.org.
- **الإصلاح:** `encodeURI()` على الروابط.
- **الدرس:** الأحرف غير ASCII في sitemap `<loc>` تحتاج percent-encoding.

---

## القسم الثاني: أخطاء تتبّع الإعلانات (2026-07-13) — الأخطر والأعمق

### 🔴 خطأ 11 (جذري): رقم Google Ads خاطئ تماماً
- **الـcommit:** `58b47da`
- **الخطأ:** الكود فيه `AW-3412658939` (مستخرج خطأً من Customer ID 341-265-8939)، لكن رقم الحساب الفعلي `AW-11081441847`.
- **كيف اكتُشف:** صورة من حساب العميل تُظهر Google Tag الحقيقي `AW-11081441847` + `GT-NMLD8SD5`.
- **السبب:** خلط بين Customer ID (معرّف الحساب) و Google Tag ID (معرّف العلامة) — **رقمان مختلفان تماماً**.
- **الإصلاح:** تصحيح المعرّف إلى `AW-11081441847`.
- **الأثر لو بقي:** جوجل يتتبّع حساباً غير موجود = صفر بيانات = «لا تتوفر علامة تتبّع».
- **الدرس الذهبي:** Customer ID ≠ Google Tag ID. لا تشتق معرّف العلامة من Customer ID — خذه من Google Tag مباشرة.

### 🔴 خطأ 12 (جذري): CSP يحجب طلبات تتبّع التحويلات
- **الـcommits:** `8886dff` (توسيع أولي) → `77fa7ed` (إكمال)
- **الخطأ:** CSP يحجب `googleads.g.doubleclick.net` (script-src) و`googletagmanager.com/td` (img-src) — طلبات viewthroughconversion والـpixel.
- **كيف اكتُشف:** Tag Assistant → console → رسائل «حظر CSP» حمراء (8 رسائل).
- **الإصلاح:** إضافة النطاقات المحجوبة لـscript-src/img-src/connect-src.
- **الأثر لو بقي:** العلامة تُحمّل لكن التحويلات **تُحجب قبل الإرسال**.
- **الدرس:** CSP الصارم قد يحجب تتبّع التحويلات صامتاً — افحص console بـTag Assistant. Google Ads يحتاج doubleclick + googletagmanager/td.

### 🔴 خطأ 13 (جذري): Conversion Label ناقص في send_to
- **الـcommits:** `fd25af1` (الصيغة) → `5519ece` (الـlabel الفعلي)
- **الخطأ:** `send_to: 'AW-11081441847'` بلا conversion label. الصيغة الرسمية تشترط `'AW-XXXX/LABEL'`.
- **كيف اكتُشف:** تحقّق Fable 5 من وثائق جوجل (answer/6331304) — أنا شككت مسبقاً واعترفت.
- **الإصلاح:** أخذ الـlabel من حساب العميل (`NIEKCOWDzPYYELfEhaQp`) وإضافته: `AW-11081441847/NIEKCOWDzPYYELfEhaQp`.
- **الأثر لو بقي:** التحويل يُرسل لكن **لا يُسجّل** (بلا label لا يرتبط بإجراء تحويل).
- **الدرس:** Google Ads conversion يحتاج label كامل. بلا label = لا قياس.

### 🔴 خطأ 14: gtag config يُحمّل بعد المحتوى (afterInteractive) بدل head
- **الـcommit:** `af96b9a`
- **الخطأ:** `<Script strategy="afterInteractive">` يضع `gtag('config','AW-...')` في نهاية body، بينما جوجل يتوقعها في `<head>`.
- **كيف اكتُشف:** مساعد Google Ads قال «العلامة غير مكتشفة» + فحصي: config موضعه 253346 (بعد `</head>` عند 8641).
- **الإصلاح:** حقن gtag الأساسي + config مباشرة في `<head>` بـlayout.tsx (inline script).
- **الأثر:** Tag Assistant يشوفها (تشتغل) لكن فحص جوجل الآلي السريع يقول «غير مكتشفة».
- **الدرس:** gtag يُفضّل في `<head>` للاكتشاف الآلي. `afterInteractive` جيد للأداء لكن قد لا يرضي crawler السريع.

### 🔴 خطأ 15: البناء انكسر بخطأ ESLint (متغير غير مستخدم)
- **الـcommit:** `82a1987`
- **الخطأ:** بعد نقل gtag لـhead، بقي `GA_MEASUREMENT_ID` معرّفاً بلا استخدام → ESLint error → `BUILD=1`.
- **كيف اكتُشف:** فحص `npm run build` بعد الـpush (كان الـpush تمّ قبل التحقق — خطأ إجرائي مني).
- **الإصلاح:** إزالة المتغير غير المستخدم فوراً → `BUILD=0` → push إصلاح.
- **الدرس المزدوج:** (1) احذف المتغيرات غير المستخدمة بعد refactor. (2) **الأهم:** لا تـpush قبل التأكد من `BUILD=0` — أنا push قبل التحقق مرة، ورصدت الفشل وأصلحته بسرعة، لكن الصح: build أخضر ثم push.

---

## القسم الثالث: قرارات واعية (رفضنا توصيات «خبيرة» لأنها لا تناسب)

من تدقيق Gemini Deep Research — رفضنا 4 توصيات بوعي:

| توصية Gemini | لماذا رُفضت |
|--------------|-------------|
| `Disallow: /_next/` في robots | يحجب أصول JS/CSS المهمة → يضر الفهرسة |
| sitemap عبر Database query | الموقع ثابت SSG بلا database أصلاً |
| Enhanced Conversions (SHA-256 phone) | مستحيل — الموقع لا يجمع رقم العميل (نقرة واتساب مباشرة) |
| @next/third-parties | لا يستحق — gtag في head يحقق نفس النتيجة، وتركيبها يخاطر بكسر تتبّع شغّال |

**الدرس:** التقرير «الخبير» قد يحوي توصيات عامة لا تناسب سياقك. حلّل كل توصية مقابل واقعك، لا تطبّق أعمى.

---

## 📊 ملخّص إحصائي

- **إجمالي commits الجلسة:** ~19
- **أخطاء جذرية (P0):** 5 (noindex، رقم Ads خاطئ، CSP يحجب، label ناقص، gtag موضع)
- **أخطاء متوسطة:** 6 (واتساب يمني، lastmod، preload، FAQ/orphan، LocalBusiness، sitemap encoding)
- **قرارات UX/تراجعات:** 2 (علامة مائية، priceRange)
- **خطأ إجرائي:** 1 (push قبل build أخضر)
- **توصيات مرفوضة بوعي:** 4

## 🏆 أهم 5 دروس دائمة

1. **Customer ID ≠ Google Tag ID** — لا تشتق معرّف العلامة من رقم الحساب.
2. **CSP الصارم يحجب التحويلات صامتاً** — افحص console بـTag Assistant.
3. **Google Ads conversion يحتاج label كامل** (`AW-XXX/LABEL`) وإلا لا يُسجّل.
4. **جوجل تتجاهل lastmod المزيّف** + Vercel shallow clone لا يدعم git per-file dates.
5. **لا تطبّق توصيات أي تقرير/وكيل أعمى** — حلّل مقابل واقعك، وتحقق حياً، ولا تنشر بلا موافقة المالك على القرارات التجارية.

---

*آخر تحديث: 2026-07-13 — يُحدّث عند كل خطأ/إصلاح جديد.*
