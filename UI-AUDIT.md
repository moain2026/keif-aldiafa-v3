# تقرير تدقيق الواجهات وSEO التقني — كيف الضيافة (keif-v2)

**التاريخ:** 2026-07-06 · **البيئة:** بناء إنتاج على `http://localhost:3170` · **المنهجية:** فحص HTML الفعلي بـ curl/grep/Python (بلا افتراضات)

الصفحات المفحوصة: `/`، `/services`، `/offerings`، `/portfolio`، `/about`، `/contact`، `/locations`، `/sababin-qahwa-jeddah`، `/qahwajiin-riyadh`، `/diyafa-munasabat-makkah` + عينات من بقية صفحات خدمة×مدينة.

---

## ✅ ما هو سليم (باختصار وإنصاف)

- كل الصفحات ترجع **200** وكل الروابط الداخلية الـ35 (بما فيها `/locations/الرياض` العربية) تعمل — لا روابط مكسورة.
- **H1 واحد فقط** في كل صفحة من الصفحات العشر (تم عدّه بـ `grep -c '<h1'` = 1 في الجميع).
- كل JSON-LD **صالح نحوياً** (تم parse بـ Python json — 0 أخطاء في 10 صفحات).
- لا توجد صور بـ `alt=""` فارغ إطلاقاً (0 في كل الصفحات).
- canonical صحيح وفريد لكل صفحة، og:image موجودة وترجع 200، sitemap يضم 39 رابطاً منها 24 صفحة خدمة×مدينة.
- الكريديت «Developed by Alabbasi Soft» موجود في **كل** الصفحات العشر (`grep -c 'Alabbasi Soft'` = 1 لكل صفحة).
- نموذج التواصل: placeholders مكتملة وواضحة (`اسمك الكريم`، `05xxxxxxxx`، …) وزر الإرسال بنص ظاهر.
- `<html lang="ar" dir="rtl">` صحيح، والنص الإنجليزي الظاهر مقصود فقط (KEIF AL-DIAFA / LUXURY HOSPITALITY / SINCE 2016 / الكريديت).

---

## 🔴 P0 — يجب الإصلاح قبل الإطلاق

### P0-1: تكرار العلامة التجارية مرتين في `<title>` لكل صفحات خدمة×مدينة (24 صفحة)
- **الصفحات:** كل `/sababin-qahwa-*`، `/qahwajiin-*`، `/diyafa-munasabat-*`
- **الوصف:** العنوان ينتهي بـ «— كيف الضيافة | كيف الضيافة» — القالب (layout template `%s | كيف الضيافة`) يضيف البراند فوق عنوانٍ يحتوي البراند أصلاً. يظهر مبتوراً وغير احترافي في نتائج Google.
- **الدليل:**
  ```
  <title>صبابين قهوة جدة | قهوجيين ومباشرين سعوديين — كيف الضيافة | كيف الضيافة</title>
  <title>ضيافة مناسبات مكة المكرمة | أعراس ومؤتمرات وفعاليات — كيف الضيافة | كيف الضيافة</title>
  ```
- **الإصلاح:** إزالة «— كيف الضيافة» من عنوان الصفحة نفسها والاكتفاء بلاحقة القالب، أو استخدام `title.absolute` في Next Metadata.

### P0-2: صفحات `qahwajiin-{city}` تحمل نفس title/description حرفياً لصفحات `sababin-qahwa-{city}` — محتوى شبه مكرر
- **الصفحات:** `/qahwajiin-riyadh` مقابل `/sababin-qahwa-riyadh` (وكل أزواج المدن الأخرى)
- **الوصف:** العنوان والوصف **متطابقان بالبايت** (md5 واحد)، وتشابه نص الصفحتين Jaccard = **0.885**. صفحتان بـ canonical مختلف ومحتوى/ميتا متطابق = خطر duplicate content، وقد تختار Google canonical بنفسها وتُسقط إحداهما من الفهرسة.
- **الدليل:**
  ```
  qahwajiin-riyadh  :: <title>صبابين قهوة الرياض | قهوجيين ومباشرين سعوديين — كيف الضيافة | كيف الضيافة</title>
  sababin-qahwa-riyadh :: <title>صبابين قهوة الرياض | ...</title>   ← متطابق تماماً
  desc md5: b3508e1a3c7458612d975290a9dfdf45 == b3508e1a3c7458612d975290a9dfdf45
  jaccard body word overlap: 0.885
  ```
  والأسوأ: H1 صفحة qahwajiin يقول «قهوجيين ومباشرين في الرياض» بينما الـ title يقول «صبابين قهوة الرياض» — **العنوان لا يطابق H1 الصفحة**.
- **الإصلاح:** إمّا كتابة title/description/محتوى متمايز فعلياً لكل قالب خدمة (قهوجيين ≠ صبابين)، أو توحيد الصفحتين بـ canonical واحد/redirect 301. الوضع الحالي أسوأ الخيارين.

---

## 🟠 P1 — مهم، يؤثر على SEO/الجودة الملموسة

### P1-1: FAQPage JSON-LD في الرئيسية بلا أسئلة ظاهرة للمستخدم
- **الصفحة:** `/`
- **الوصف:** يوجد schema بـ 4 أسئلة (`ما هي خدمات كيف الضيافة؟` …) لكن **لا يوجد أي قسم FAQ مرئي** في الصفحة. هذا مخالف صريح لإرشادات Google للبيانات المنظمة (المحتوى المنظم يجب أن يكون مرئياً) وقد يؤدي لإجراء يدوي.
- **الدليل:**
  ```
  grep '"@type":"Question"' home.html → 4 نتائج (داخل <script> فقط)
  Python: FAQ question visible in body (خارج <script>) → False
  grep -c 'أسئلة شائعة' home.html → 0
  ```
- **الإصلاح:** إضافة قسم «أسئلة شائعة» مرئي في الرئيسية بنفس الأسئلة، أو حذف FAQPage schema منها. (صفحات المدن سليمة — أسئلتها ظاهرة في `<h3>`.)

### P1-2: رابط ميت `href="#"` في فوتر كل الصفحات
- **الصفحات:** الكل (10/10)
- **الوصف:** عنصر «جميع مناطق المملكة العربية السعودية» في الفوتر رابط `<a href="#">` لا يذهب لأي مكان — والأولى أن يشير إلى `/locations`.
- **الدليل:**
  ```
  grep -c 'href="#"' *.html → 1 في كل صفحة
  href="#" ... <span>جميع مناطق المملكة العربية السعودية</span>
  ```
- **الإصلاح:** `href="/locations"` أو تحويله إلى `<span>` غير قابل للنقر.

### P1-3: نصوص alt إنجليزية مشتقة من أسماء الملفات — ركيكة ومكررة داخل موقع عربي
- **الصفحات:** صفحات خدمة×مدينة الثلاث (معارض «من أعمالنا»)
- **الوصف:** alt مثل «خدمات الضيافة — Cleaning f» و«— Dagla janbiya» و«— Safarjia f» و«— Female» — كلمات إنجليزية مقطوعة من أسماء ملفات، بعضها مكرر (Female ×3، Photo booth ×2، Safarjia ×2، Dagla ×2). alt بلا قيمة وصفية ولا SEO، وقارئ الشاشة العربي سيقرأ هراءً.
- **الدليل:**
  ```
  qahwajiin-riyadh.html:  3 alt="خدمات الضيافة — Female"   2 alt="خدمات الضيافة — Photo booth"
  sababin-qahwa-jeddah.html: alt="خدمات الضيافة — Dagla janbiya"  alt="خدمات الضيافة — Safarjia f"
  ```
- **الإصلاح:** خريطة alt عربية وصفية لكل صورة (مثال: «صبّاب قهوة بزي مكاوي تراثي في حفل زواج بجدة»).

### P1-4: العنوان المرئي H1 في هيرو الرئيسية صغير بصرياً — التسلسل البصري معكوس
- **الصفحة:** `/` (البطاقة الأمامية/الهيرو)
- **الوصف:** الـ`<h1>` الحقيقي («قهوجيين وصبابين قهوة لضيافة فاخرة في السعودية») مضبوط على `font-size:clamp(1.05rem, 2.6vw, 1.35rem)` — أي بحجم نص عادي تقريباً — بينما الكلمة الضخمة بصرياً «كيف الضيافة» هي `<p>`. دلالياً مقبول لـSEO لكن UX/بصرياً العنوان الأهم يبدو سطراً ثانوياً باهتاً، وعناوين بقية الصفحات الداخلية تصل `clamp(2rem,6vw,3.5rem)` فيظهر عدم اتساق واضح بين هيرو الرئيسية والداخلية. بقية البطاقة سليمة ومكتملة: شارة SINCE 2016 + الاسم + p وصفي كامل + 3 وسوم (فعاليات رسمية/مؤتمرات دولية/مناسبات خاصة) + زرا CTA صحيحان («احجز الآن» → wa.me/966508252134، «اكتشف خدماتنا» → /services).
- **الدليل:**
  ```
  <h1 ... style="font-size:clamp(1.05rem, 2.6vw, 1.35rem);font-weight:700...">قهوجيين وصبابين قهوة لضيافة فاخرة في السعودية</h1>
  <p>كيف الضيافة</p>  ← هذا هو النص الأضخم بصرياً
  ```
- **الإصلاح:** تكبير الـH1 درجة أو درجتين (≈1.5–1.9rem) وزيادة تباينه، أو دمج «كيف الضيافة» داخل الـH1 كـ`<span>`.

### P1-5: قفزات في تسلسل العناوين h1→h3 (بدون h2)
- **الصفحات:** `/locations` (h1 ثم 4×h3 مباشرة، **صفر h2**)، `/services` (h1 → h3 بطاقات الخدمات)، `/offerings` (h1 → h3 بطاقات المشروبات)
- **الدليل:**
  ```
  locations.html: 1×<h1>, 0×<h2>, 4×<h3>
  services.html:  <h1> ثم مباشرة <h3>قهوجيين وصبابين قهوة ومباشرين
  offerings.html: <h1> ثم مباشرة <h3>القهوة التركية
  ```
- **الإصلاح:** ترقية عناوين البطاقات إلى h2، أو إضافة h2 قسمي فوقها.

---

## 🟡 P2 — تحسينات مستحسنة

### P2-1: تضخم JSON-LD في ImageGallery — تكرار Organization كاملة لكل صورة
- **الصفحات:** `/offerings` (122×ImageObject + 122×Organization!)، `/portfolio` (107+107)، `/services` (97+97)
- **الدليل:** `grep -c '"@type":"Organization"' offerings.html` داخل ld+json → 122. كل ImageObject يحمل نسخة كاملة من بيانات المؤسسة، ما يضخّم HTML (‎217KB للصفحة) بلا فائدة SEO.
- **الإصلاح:** استخدام `"author": {"@id": "https://keifaldiafa.com/#org"}` بالمرجع بدل التضمين، أو حذف author من كل صورة.

### P2-2: `areaServed` في Service schema = Country بينما الصفحة لمدينة محددة
- **الصفحات:** صفحات خدمة×مدينة
- **الدليل:** `qahwajiin-riyadh → areaServed: {"@type":"Country","name":"Saudi Arabia"}` — صفحة الرياض يجب أن تخدم `{"@type":"City","name":"الرياض"}` لتعظيم إشارة Local SEO.
- **الإصلاح:** تمرير المدينة إلى areaServed في قالب الصفحة.

### P2-3: alt شعارات الشركاء يحوي كلمة "Alt" حرفياً ومكرر ×4
- **الصفحة:** `/` (شريط شركاء النجاح)
- **الدليل:** `4 alt="WSM Digital"`, `4 alt="WSM Digital Alt"`, `alt="Olayan Group Alt"`, `alt="Damanat Alt"` — نسخ الـmarquee تكرر الصور، والنسخ البديلة سُمّيت «… Alt» (اسم ملف تسرّب للـalt). الأصح: نسخ الـmarquee المكررة تأخذ `aria-hidden="true"` و`alt=""`، والشعار يوصف مرة واحدة («شعار مجموعة العليان»).

### P2-4: تناقض رقم سنوات الخبرة: «أكثر من 8 سنوات» مقابل «SINCE 2016»
- **الصفحات:** `/about` (‏8 مرات «أكثر من 8») و`/` (شارة SINCE 2016 + «٨ سنوات»)
- **الدليل:** `grep`: about.html ×8 «أكثر من 8»، home ×1 «SINCE 2016». نحن في 2026 → منذ 2016 = **10 سنوات**، لا 8. أحد الرقمين قديم.
- **الإصلاح:** حساب السنوات ديناميكياً `new Date().getFullYear() - 2016` أو توحيد الرقم.

### P2-5: حقول نموذج التواصل بلا ربط label↔input
- **الصفحة:** `/contact`
- **الدليل:** `grep -c '<label[^>]*for=' contact.html` → **0**، و`<input ... id=` → **0**. الـlabels موجودة نصياً لكن غير مرتبطة برمجياً — قارئات الشاشة لن تنطق اسم الحقل عند التركيز، والنقر على الـlabel لا يفعّل الحقل.
- **الإصلاح:** إضافة `id` لكل input و`htmlFor` لكل label.

### P2-6: og:image واحدة عامة لكل الصفحات
- **الدليل:** كل الصفحات العشر → `og:image = https://keifaldiafa.com/og-image.jpg`. تعمل (200) لكن صفحات المدن تستفيد من صور OG مخصصة (اسم المدينة على الصورة) لرفع CTR عند المشاركة. تحسين اختياري.

### P2-7: H1 صفحات المدن حشوٌ تكراري
- **الدليل:** `<h1>قهوجيين ومباشرين في الرياض — قهوجيين ومباشرين وصبابين قهوة سعوديين</h1>` — «قهوجيين ومباشرين» مرتان في نفس الـH1. يُقرأ كحشو كلمات مفتاحية. صياغة أنظف: «قهوجيين ومباشرين وصبابين قهوة سعوديون في الرياض».

---

## أهم 5 مشاكل (مرتبة)

1. **P0-2** — صفحات `qahwajiin-*` نسخ شبه طبق الأصل من `sababin-qahwa-*` بعناوين/أوصاف متطابقة بالبايت (خطر إسقاط من الفهرسة + title لا يطابق H1).
2. **P0-1** — «كيف الضيافة | كيف الضيافة» مكررة في title كل الـ24 صفحة خدمة×مدينة.
3. **P1-1** — FAQPage schema في الرئيسية بلا محتوى مرئي (مخالفة إرشادات Google الصريحة).
4. **P1-3** — alt إنجليزي مبتور من أسماء ملفات («Cleaning f»، «Safarjia f») في معارض صفحات المدن.
5. **P1-2 + P1-4** — رابط `href="#"` ميت في فوتر كل الصفحات، وH1 الهيرو صغير بصرياً بشكل يقلب الهرم البصري.

## التقييم العام /10

| الصفحة | التقييم | السبب الرئيسي |
|---|---|---|
| `/` (الرئيسية) | **7/10** | FAQ schema بلا محتوى مرئي، H1 صغير بصرياً، alt «… Alt»، رابط # ميت — البطاقة الأمامية نفسها مكتملة العناصر (شارة+عنوان+وصف+وسوم+CTA×2 صحيحة) |
| `/services` | **8/10** | سليمة عموماً؛ قفزة h1→h3 وتضخم JSON-LD (97 Organization) |
| `/offerings` | **7.5/10** | قفزة h1→h3، أثقل JSON-LD (122 Organization مكررة، 217KB) |
| `/portfolio` | **8/10** | نظيفة؛ تضخم JSON-LD (107) فقط |
| `/about` | **8/10** | جيدة؛ تناقض «8 سنوات» مع SINCE 2016 |
| `/contact` | **7.5/10** | placeholders ممتازة لكن labels غير مرتبطة بالحقول (a11y) |
| `/locations` | **7/10** | صفر h2 (h1→h3 مباشرة)، محتوى نصي خفيف نسبياً |
| `/sababin-qahwa-jeddah` | **6.5/10** | title مزدوج البراند، alt إنجليزي مبتور — البنية والـFAQ المرئي ممتازان |
| `/qahwajiin-riyadh` | **5.5/10** | **الأضعف**: title/desc منسوخان حرفياً من صفحة صبابين + لا يطابقان H1 + تشابه محتوى 88.5% |
| `/diyafa-munasabat-makkah` | **7.5/10** | الوحيدة بميتا متمايزة فعلاً بين القوالب الثلاثة؛ يبقى تكرار البراند في title |

**خلاصة:** الأساس التقني قوي (روابط سليمة، JSON-LD صالح، H1 مفرد، canonical صحيح، RTL نظيف)، لكن قالب صفحات خدمة×مدينة أُطلق باستعجال: ميتا منسوخة بين القوالب، براند مكرر في العناوين، وalt من أسماء الملفات. إصلاح P0 الاثنين + P1-1 يرفع الموقع درجة كاملة.
