# تقرير أرشفة وفهرسة الصور — كيف الضيافة
### Image SEO / Archiving & Indexing Report — Keif Al-Diafa

**الموقع:** https://keifaldiafa.com
**التاريخ:** 2026-07-06
**المرجعية:** المعايير الرسمية الحالية من Google Search Central (وليست المصادر القديمة).

---

## 1. الملخص التنفيذي

تمّت إضافة طبقة **أرشفة وفهرسة صور كاملة** للموقع بحيث تصبح **كل صور الموقع قابلة للاكتشاف والظهور والبحث في Google صور (Google Images)**، وفق أحدث معايير جوجل الرسمية.

| المؤشر | القيمة |
|--------|--------|
| إجمالي الصور القابلة للفهرسة (WebP) | **388** |
| ملفات SVG (شعارات/علامات مائية — مستثناة عمداً) | 5 |
| عدد الصور في خريطة الصور (`<image:loc>`) | **388** (100%) |
| صفحات الهبوط المرتبطة بالصور | 4 (`/`، `/services`، `/offerings`، `/portfolio`) |
| صيغة الصور | WebP (صيغة حديثة يدعمها جوجل ✔) |
| حالة البِلد | ✅ `tsc --noEmit` = 0 أخطاء ، `npm run build` = نجاح (13/13 صفحة) |

---

## 2. ما الذي يجعل الصور تُؤرشَف وتظهر في البحث؟ (خلفية)

بحسب [وثيقة جوجل الرسمية لـ SEO الصور](https://developers.google.com/search/docs/appearance/google-images)، تعتمد فهرسة الصور على:
1. **وسم `<img>` حقيقي** (جوجل لا يفهرس صور خلفية CSS).
2. **خريطة موقع للصور** (Image Sitemap) لاكتشاف الصور — خصوصاً التي تُحمَّل عبر JavaScript.
3. **نص بديل (alt text)** وصفي طبيعي — أهم إشارة، مع تجنّب حشو الكلمات.
4. **أسماء ملفات وصفية** قصيرة ودالة.
5. **بيانات منظّمة (Structured Data)** لأهلية النتائج الغنية والشارة في بحث الصور.
6. **صورة ممثّلة للصفحة** عبر `primaryImageOfPage` / `og:image`.
7. **سرعة وجودة** (Core Web Vitals + صور متجاوبة).

---

## 3. ما الذي نُفّذ فعلياً

### 3.1 خريطة موقع الصور — `/image-sitemap.xml`
- Route Handler جديد: `src/app/image-sitemap.xml/route.ts`.
- يستخدم فضاء الأسماء الرسمي: `xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"`.
- يجمع الصور تحت صفحة الهبوط المناسبة (`<url><loc>` ثم `<image:image><image:loc>…</image:loc>`).
- **يشمل كل الـ 388 صورة** بروابط مطلقة.

> ⚠️ **ملاحظة معيارية مهمة:** ألغت جوجل رسمياً (Spring 2022) الوسوم `<image:title>` و`<image:caption>` و`<image:geo_location>` و`<image:license>` من مواصفة خريطة الصور — لذلك لم نستخدمها إطلاقاً؛ اكتفينا بـ `<image:loc>` فقط (المعيار الحالي الصحيح)، ونقلنا العناوين/الأوصاف إلى النص البديل والبيانات المنظّمة حيث يقرأها جوجل فعلاً. (المصادر التي ما زالت تنصح بها قديمة.)

### 3.2 فهرس خرائط المواقع — `/sitemap_index.xml`
- Route Handler جديد: `src/app/sitemap_index.xml/route.ts`.
- يجمع `sitemap.xml` (الصفحات) + `image-sitemap.xml` (الصور) في نقطة دخول واحدة — هذا هو الرابط المُوصى بتقديمه في Search Console.

### 3.3 تحديث `robots.txt` — `src/app/robots.ts`
- يشير الآن إلى خرائط المواقع الثلاث (الصفحات + الصور + الفهرس)، مع الإبقاء على قواعد الحجب (`/api/`، `/admin/`، `/private/`).

### 3.4 كتالوج الصور — `src/lib/imageCatalog.ts`
- مصدر الحقيقة = مسح فعلي لملفات `public/images/**` (يضمن عدم إغفال أي صورة).
- يشتق نصاً وصفياً **طبيعياً ثنائي اللغة** (سياق عربي + كلمات مفتاحية إنجليزية من أسماء الملفات الوصفية) بحدّ ~7 كلمات — **بلا حشو**.
- يربط كل صورة بصفحة الهبوط الأنسب.
- دوال: `getAllImages()`، `getImagesByPage()`، `getImagesForPage(path)`، `filenameToText()`.

### 3.5 البيانات المنظّمة — `src/lib/schema.ts`
- `generateImageGallerySchema()` جديدة: تُنتج `ImageGallery` تحوي `ImageObject` لكل صورة مع `contentUrl` (شرط جوجل للأهلية) + `name` + `caption` + بيانات الحقوق/المُنشئ.
- `generateWebPageSchema()` أُضيف لها `primaryImageOfPage` (توصية جوجل لتحديد الصورة الممثّلة).
- **حُقنت JSON-LD** في صفحات `/portfolio` و`/offerings` و`/services` (107+ ImageObject على البورتفوليو وحده).

### 3.6 النص البديل (alt)
- الصور تُعرض عبر `next/image` (وسم `<img>` حقيقي قابل للفهرسة، لا خلفيات CSS) — تحقّقنا من ذلك.
- النص البديل يُشتق من الكتالوج/أسماء الملفات الوصفية بشكل طبيعي.

---

## 4. الروابط التي سيزحف إليها جوجل

| الرابط | الغرض |
|--------|-------|
| `https://keifaldiafa.com/robots.txt` | يوجّه الزواحف ويعلن خرائط المواقع |
| `https://keifaldiafa.com/sitemap.xml` | خريطة الصفحات (6 صفحات) |
| `https://keifaldiafa.com/image-sitemap.xml` | **خريطة الصور — 388 صورة** |
| `https://keifaldiafa.com/sitemap_index.xml` | فهرس يجمع الاثنين (قدّمه في Search Console) |

---

## 5. كيفية التقديم في Google Search Console

1. ادخل [Google Search Console](https://search.google.com/search-console) وتحقّق من ملكية `keifaldiafa.com`.
2. من القائمة الجانبية: **Sitemaps (خرائط المواقع)**.
3. أضف: `sitemap_index.xml` ثم `image-sitemap.xml`.
4. راقب تقرير **الفهرسة → الصفحات** وتقرير **خرائط المواقع** للتأكد من قراءة الصور بلا أخطاء.
5. لظهور الصور في البحث: قد يستغرق الأمر أياماً إلى أسابيع حسب زحف جوجل.

---

## 6. قائمة تدقيق مقابل معايير جوجل لـ SEO الصور

| المعيار | الحالة |
|---------|:------:|
| أسماء ملفات وصفية قصيرة ودالة | ✅ (كانت مطبّقة أصلاً) |
| نص بديل (alt) وصفي طبيعي بلا حشو كلمات | ✅ |
| وسوم `<img>` حقيقية (لا خلفيات CSS) | ✅ (عبر next/image) |
| خريطة موقع للصور بالمواصفة الحالية | ✅ (388 صورة، `image:loc` فقط) |
| فهرس خرائط مواقع + robots محدّث | ✅ |
| بيانات منظّمة ImageObject + contentUrl | ✅ |
| `primaryImageOfPage` لكل صفحة | ✅ |
| `og:image` / Open Graph | ✅ (كان مطبّقاً في layout) |
| صيغة صور حديثة (WebP) | ✅ |
| صور متجاوبة + تحميل كسول (lazy) | ✅ (next/image) |
| سرعة/Core Web Vitals (CLS/LCP) | ✅ (عولجت في مرحلة الأداء) |
| تجنّب حشو الكلمات المفتاحية | ✅ |

---

## 7. القيود المُحترمة أثناء التنفيذ
- لم يتم المساس بـ `src/app/locations/` ولا `Footer.tsx` ولا أي ألوان/نصوص/كلمات مفتاحية.
- لم يتم تغيير إعدادات الصور في `next.config.js` ولا إعادة `remotePatterns`.
- البِلد بقي أخضر (tsc + build).
